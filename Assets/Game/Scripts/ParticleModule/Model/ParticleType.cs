using System.Collections.Generic;
using UnityEngine;

namespace Game.ParticleModule.Config
{
    public enum ParticleType
    {
        SwapEffect = 0,
        SwapSameEffect = 1,
        MergeEffect = 2,
    }
}